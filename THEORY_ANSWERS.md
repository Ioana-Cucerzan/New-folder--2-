# Theory answers

## Part I.1 — Output prediction
1) [1, 2, 3, 4] 
2)  [1]
    [1,2] 
3)  cba bc
4)  7 False
5)  0
    2

## Part I.2 — Short questions
1)  List = mutable (se poate modifica)
    Tuple = immutable (nu se poate modifica)
2)  Un side efect e atunci cand o functie schimba ceva si in afara ei
3)  O(n) inseamna ca timpul de executie al unui algoritm creste proportional cu marimea datelor
4)  __init__ este constructorul unei clase. El se executa automat cand se creeaza un obiect si initializeaza atributele acestuia.

5)  Folosesc try/except atunci cand un cod poate da eroare ii vreau ca programul sa continue fara sa se opreasca.

